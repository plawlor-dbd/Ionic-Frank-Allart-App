import { PopoverController } from '@ionic/angular';
export declare class IonicContextMenuComponent {
    private popoverCtrl;
    popover: HTMLIonPopoverElement;
    icon: string;
    constructor(popoverCtrl: PopoverController);
    present(child: any): Promise<void>;
}
